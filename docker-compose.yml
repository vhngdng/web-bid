version: '3.9'
services:
  backend:
    container_name: bid-backend-server
    image: vuhoangdung/web-bid:bid-backend-server
    depends_on:
      - postgre
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./backend/demo/spring-resource-server/src:/usr/src/web-bid-app/src
    ports:
      - 8080:8080
      - 5432
    networks:
      - web-bid-network

  frontend:
    container_name: bid-frontend-server
    image: vuhoangdung/web-bid:bid-frontend-server
    env_file: .env
    volumes:
      - ./front-end:/app
    restart: unless-stopped
    ports:
      - 3000:3000
    networks:
      - web-bid-network

  nginx:
    image: nginx:latest
    ports:
      - 8081:80
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf
    networks:
      - web-bid-network
    depends_on:
      - frontend
      - backend
    restart: unless-stopped

  postgre:
    image: postgres:15.2
    container_name: db
    env_file: .env
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRE_PASSWORD}
      - POSTGRES_DB=web-project
    volumes:
      - ./dbdata:/var/lib/postgres
      # - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5433:5432"
    restart: unless-stopped
    networks:
      - web-bid-network
networks:
  web-bid-network:
    driver: bridge

